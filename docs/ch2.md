# บทที่ 2 การวิเคราะห์ความต้องการ

## **2.1 การเก็บรวบรวมความต้องการ**

### 2.1.1 วิธีการเก็บข้อมูล

กลุ่มผู้พัฒนาได้ดำเนินการเก็บรวบรวมความต้องการของระบบโดยใช้วิธีการหลากหลาย ได้แก่

**1. การสัมภาษณ์ (Interview)**

กลุ่มผู้พัฒนาได้สัมภาษณ์เจ้าหน้าที่ห้องปฏิบัติการ CKLab Computer Center เพื่อรวบรวมปัญหาที่เกิดขึ้นจริงในการบริหารจัดการห้องปฏิบัติการ ประเด็นสำคัญที่ได้จากการสัมภาษณ์ ได้แก่
- ปัญหาความล่าช้าและการกรอกข้อมูลซ้ำซ้อนในระบบ Google Forms เดิม
- ความต้องการระบบติดตามสถานะเครื่องคอมพิวเตอร์แบบเรียลไทม์
- ความต้องการระบบจองเครื่องล่วงหน้าที่รองรับการนำเข้าข้อมูลจากไฟล์ CSV
- ความต้องการรายงานสถิติการใช้งานที่ครบถ้วนเพื่อสนับสนุนการตัดสินใจ

**2. การสังเกตการณ์ (Observation)**

กลุ่มผู้พัฒนาได้สังเกตการณ์การใช้งานห้องปฏิบัติการจริง เพื่อทำความเข้าใจกระบวนการทำงาน (Workflow) ของผู้ใช้งานและเจ้าหน้าที่ ซึ่งพบว่า
- กระบวนการ Check-in ด้วย Google Forms ใช้เวลาเฉลี่ย 2–3 นาทีต่อคน ซึ่งส่งผลให้เกิดการรอคิวในช่วงที่มีผู้ใช้งานหนาแน่น
- เจ้าหน้าที่ต้องตรวจสอบสถานะเครื่องคอมพิวเตอร์ด้วยสายตาซึ่งเกิดความผิดพลาดได้ง่าย
- ผู้ใช้บางรายออกจากห้องปฏิบัติการโดยไม่ได้ทำการบันทึกการออก (Check-out)

**3. การศึกษาระบบเดิม (Existing System Analysis)**

กลุ่มผู้พัฒนาได้ศึกษาและวิเคราะห์ระบบ Google Forms เดิมที่ใช้งานอยู่ เพื่อระบุจุดบกพร่องและนำมาพัฒนาให้ดียิ่งขึ้น โดยพบปัญหาหลัก ดังนี้

| ปัญหาของระบบเดิม | ผลกระทบ |
| :--- | :--- |
| ผู้ใช้ต้องกรอกข้อมูลส่วนตัวทุกครั้ง | เสียเวลา เกิด Human Error |
| ไม่มีระบบยืนยันตัวตน | ข้อมูลไม่ถูกต้อง |
| ไม่มีการติดตามสถานะเครื่องแบบเรียลไทม์ | เจ้าหน้าที่ไม่ทราบสถานะจริง |
| ไม่มีระบบจองล่วงหน้า | เกิดการใช้งานซ้ำซ้อน |
| ไม่มีรายงานสถิติอัตโนมัติ | วิเคราะห์ข้อมูลได้ยาก |

### 2.1.2 ผู้มีส่วนได้ส่วนเสีย (Stakeholders)

ผู้มีส่วนได้ส่วนเสียของระบบ CKLab ประกอบด้วยกลุ่มบุคคลต่อไปนี้

| กลุ่ม | บทบาทในระบบ | ความต้องการหลัก |
| :--- | :--- | :--- |
| **นักศึกษา** | ผู้ใช้งานหลักของห้องปฏิบัติการ | Check-in/out ที่รวดเร็ว, จองเครื่องล่วงหน้า |
| **บุคลากร (อาจารย์/เจ้าหน้าที่)** | ผู้ใช้งานภายในมหาวิทยาลัย | Check-in/out ด้วยรหัสบุคลากร |
| **บุคคลภายนอก (Guest)** | ผู้มาเยี่ยมชมหรือผู้รับบริการทั่วไป | Check-in โดยไม่ต้องมีรหัส UBU |
| **เจ้าหน้าที่ห้องปฏิบัติการ** | ผู้ดูแลระบบและบริหารทรัพยากร | Monitor Dashboard, Booking Management, Reports |

---

## **2.2 ความต้องการเชิงหน้าที่ (Functional Requirements)**

ความต้องการเชิงหน้าที่ของระบบ CKLab แบ่งออกตามกลุ่มฟังก์ชันการทำงาน ดังนี้

### 2.2.1 กลุ่มฟังก์ชัน Kiosk (หน้าจอสาธารณะ)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-01 | Check-in ด้วยรหัส UBU WiFi | ผู้ใช้กรอกรหัส UBU WiFi ระบบเชื่อมต่อ UBU API เพื่อยืนยันตัวตนและดึงข้อมูล (ชื่อ, คณะ, ชั้นปี) โดยอัตโนมัติ | สูง |
| FR-02 | Check-in โหมด Guest | บุคคลภายนอกสามารถ Check-in ได้โดยไม่ต้องมีรหัส UBU WiFi โดยกรอกข้อมูลด้วยตนเอง | สูง |
| FR-03 | Check-out ออกจากระบบ | ผู้ใช้สามารถสิ้นสุดเซสชันการใช้งาน ระบบบันทึกเวลาสิ้นสุดและคืนสถานะเครื่องเป็น AVAILABLE | สูง |
| FR-04 | แสดง Timer เซสชัน | หน้าจอแสดงเวลาที่ใช้งานสะสม (HH:MM:SS) แบบเรียลไทม์หลัง Check-in สำเร็จ | สูง |
| FR-05 | เก็บ Feedback หลังใช้งาน | หลัง Check-out ระบบแสดงแบบประเมินความพึงพอใจ (1–5 ดาว) พร้อมช่องข้อเสนอแนะ (ไม่บังคับ) | กลาง |

### 2.2.2 กลุ่มฟังก์ชัน Monitor Dashboard (แดชบอร์ดสำหรับผู้ดูแล)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-06 | แสดงสถานะเครื่องคอมพิวเตอร์แบบเรียลไทม์ | Dashboard แสดงสถานะทุกเครื่อง (ว่าง / ใช้งาน / จอง / แจ้งซ่อม) พร้อมชื่อผู้ใช้งานและเวลาที่ใช้งาน โดยอัปเดตอัตโนมัติผ่าน AJAX | สูง |
| FR-07 | Admin Force Check-in / Force Check-out | ผู้ดูแลระบบสามารถ Check-in หรือสิ้นสุดเซสชันแทนผู้ใช้ได้จาก Dashboard กรณีที่ผู้ใช้ลืม Check-out | สูง |

### 2.2.3 กลุ่มฟังก์ชัน Booking (การจองล่วงหน้า)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-08 | จองเครื่องคอมพิวเตอร์ล่วงหน้า | ผู้ดูแลระบบสามารถสร้างการจองระบุเครื่อง, ช่วงวันเวลา และรหัสผู้ใช้ พร้อม Workflow การอนุมัติ (PENDING → APPROVED / REJECTED) | สูง |
| FR-09 | นำเข้าข้อมูลการจองจาก CSV | รองรับการนำเข้าข้อมูลการจองจำนวนมากผ่านไฟล์ CSV (Header ภาษาไทย: วันที่, เวลา, ผู้จอง, เครื่อง) | กลาง |

### 2.2.4 กลุ่มฟังก์ชัน PC Management (จัดการเครื่องคอมพิวเตอร์)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-10 | จัดการข้อมูลเครื่องคอมพิวเตอร์ | รองรับการเพิ่ม แก้ไข ลบข้อมูลเครื่องคอมพิวเตอร์ กำหนดสถานะ และผูกซอฟต์แวร์ประจำเครื่อง | สูง |

### 2.2.5 กลุ่มฟังก์ชัน Software Management (จัดการซอฟต์แวร์)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-11 | จัดการรายการซอฟต์แวร์และเครื่องมือ AI | รองรับการเพิ่ม แก้ไข ลบรายการซอฟต์แวร์ แยกประเภท (Software / AI Tool) ติดตามวันหมดอายุสัญญาอนุญาต | กลาง |

### 2.2.6 กลุ่มฟังก์ชัน Reports & Analytics (รายงานและวิเคราะห์)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-12 | ดูรายงานและส่งออกข้อมูล CSV | แสดงสถิติการใช้งาน กรองตามประเภทผู้ใช้ (นักศึกษา/บุคลากร/Guest) และช่วงเวลา รองรับการส่งออกเป็น CSV (UTF-8 BOM สำหรับ Excel ภาษาไทย) | สูง |

### 2.2.7 กลุ่มฟังก์ชัน System Config (ตั้งค่าระบบ)

| รหัส | ความต้องการ | รายละเอียด | ลำดับความสำคัญ |
| :---: | :--- | :--- | :---: |
| FR-13 | ตั้งค่าระบบและประกาศ | กำหนดชื่อห้อง, สถานที่, สถานะเปิด/ปิดบริการ, เปิด/ปิดการจอง, และข้อความประกาศที่แสดงบนหน้า Kiosk | กลาง |
| FR-14 | จัดการบัญชีผู้ดูแลระบบ | รองรับการเพิ่ม แก้ไข ปิดการใช้งาน และลบบัญชีผู้ดูแล พร้อมป้องกันการลบบัญชีตนเอง | สูง |

---

## **2.3 ความต้องการเชิงไม่ใช่หน้าที่ (Non-Functional Requirements)**

| รหัส | ประเภท | ความต้องการ | เกณฑ์วัด |
| :---: | :--- | :--- | :--- |
| NFR-01 | ประสิทธิภาพ (Performance) | หน้า Monitor Dashboard ต้องอัปเดตสถานะภายใน 3 วินาที | Response time ของ AJAX API ≤ 2 วินาที |
| NFR-02 | ความปลอดภัย (Security) | ทุก URL ของ Admin Portal ต้องผ่านการยืนยันตัวตน | ใช้ LoginRequiredMixin, CSRF Token, Password Hashing (Django pbkdf2) |
| NFR-03 | ความง่ายในการใช้งาน (Usability) | หน้า Kiosk ต้องใช้งานได้โดยไม่ต้องอ่านคู่มือ | ผ่านการทดสอบกับผู้ใช้จริง ≥ 5 คน |
| NFR-04 | ความพร้อมใช้งาน (Reliability) | ระบบต้องจัดการ "Ghost State" (เครื่องค้างสถานะ IN_USE โดยไม่มีเซสชัน) ได้อัตโนมัติ | ตรวจสอบและแก้ไขสถานะอัตโนมัติเมื่อมีการเข้าถึง |
| NFR-05 | ความสามารถในการบำรุงรักษา (Maintainability) | โครงสร้างโค้ดต้องแบ่งแยกความรับผิดชอบชัดเจน | ใช้ MVT Pattern, แยกไฟล์ Views ตาม module |
| NFR-06 | ความสามารถในการรองรับภาษาไทย | ทุกส่วนของ UI ต้องแสดงภาษาไทยได้ถูกต้อง | ไฟล์ CSV Export ใช้ UTF-8 BOM, ข้อความแจ้งเตือนเป็นภาษาไทย |

---

## **2.4 Use Case Diagram**

ไดอะแกรม Use Case แสดงภาพรวมของฟังก์ชันการทำงานของระบบ CKLab แบ่งตามกลุ่มผู้ใช้ได้เป็น 2 กลุ่มหลัก คือ ผู้ใช้งานทั่วไป (User) ที่เข้าถึงระบบผ่านหน้าจอ Kiosk และผู้ดูแลระบบ (Admin) ที่เข้าถึงผ่าน Admin Portal

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ระบบ CKLab Management System                      │
│                                                                       │
│  ┌─ Kiosk ──────────────────────┐  ┌─ Admin Portal ───────────────┐  │
│  │  (UC-01) Check-in UBU WiFi   │  │  (UC-06) เข้าสู่ระบบ Admin  │  │
│  │  (UC-02) Check-in Guest      │  │  (UC-07) Monitor Dashboard   │  │
│  │  (UC-03) ดู Timer เซสชัน    │  │  (UC-08) Force Check-in/out  │  │
│  │  (UC-04) Check-out           │  │  (UC-09) จัดการ Booking      │  │
│  │  (UC-05) ให้ Feedback        │  │  (UC-10) Import CSV Booking  │  │
│  └──────────────────────────────┘  │  (UC-11) จัดการ PC           │  │
│                                     │  (UC-12) จัดการ Software     │  │
│  👤 User (นักศึกษา/บุคลากร/Guest) │  (UC-13) ดูรายงาน & Export  │  │
│                                     │  (UC-14) ตั้งค่าระบบ/Admin  │  │
│                                     └──────────────────────────────┘  │
│                                     👤 Admin (ผู้ดูแลระบบ)            │
└─────────────────────────────────────────────────────────────────────┘
```

*รูปที่ 2.1 Use Case Diagram ของระบบ CKLab Management System แสดงความสัมพันธ์ระหว่างกลุ่มผู้ใช้ (User และ Admin) กับฟังก์ชันการทำงานทั้งหมดของระบบ*

---

## **2.5 คำอธิบาย Use Case (Use Case Description)**

### UC-01: Check-in ด้วยรหัส UBU WiFi

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-01 |
| **ชื่อ Use Case** | Check-in ด้วยรหัส UBU WiFi |
| **Actor** | ผู้ใช้งานภายใน (นักศึกษา / บุคลากร) |
| **เงื่อนไขก่อนเริ่ม (Precondition)** | เครื่องคอมพิวเตอร์มีสถานะ AVAILABLE และระบบ CKLab เปิดให้บริการ (is_open = True) |
| **เหตุกระตุ้น (Trigger)** | ผู้ใช้กดปุ่ม "เช็คอิน (UBU WiFi)" บนหน้าจอ Kiosk |
| **Main Flow** | 1. ระบบแสดงหน้าจอสำหรับกรอกรหัส UBU WiFi<br>2. ผู้ใช้กรอกรหัส UBU WiFi<br>3. ระบบส่งรหัสที่เข้ารหัส Base64 ไปยัง UBU API<br>4. UBU API ตอบกลับข้อมูล (ชื่อ, คณะ, ชั้นปี, ประเภทผู้ใช้)<br>5. ระบบสร้างรายการ UsageLog พร้อมบันทึก start_time อัตโนมัติ<br>6. ระบบเปลี่ยนสถานะ Computer เป็น IN_USE<br>7. ระบบแสดงหน้า Timer แสดงเวลาการใช้งานแบบเรียลไทม์ |
| **Alternate Flow** | 3a. UBU API ไม่ตอบสนอง → ระบบแสดงข้อความแจ้งเตือนภาษาไทย ให้ผู้ใช้ลองใหม่หรือเลือกโหมด Guest |
| **เงื่อนไขหลังสิ้นสุด (Postcondition)** | UsageLog ถูกสร้างใหม่ สถานะ Computer เปลี่ยนเป็น IN_USE ผู้ใช้เห็นหน้า Timer |

---

### UC-02: Check-in โหมด Guest

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-02 |
| **ชื่อ Use Case** | Check-in โหมด Guest |
| **Actor** | บุคคลภายนอก (Guest) |
| **เงื่อนไขก่อนเริ่ม** | เครื่องคอมพิวเตอร์มีสถานะ AVAILABLE และระบบ CKLab เปิดให้บริการ |
| **เหตุกระตุ้น** | ผู้ใช้กดปุ่ม "เข้าใช้งานแบบ Guest" บนหน้าจอ Kiosk |
| **Main Flow** | 1. ระบบแสดงหน้าจอสำหรับกรอกข้อมูลส่วนตัว (ชื่อ, ประเภทผู้ใช้)<br>2. ผู้ใช้กรอกข้อมูลด้วยตนเอง<br>3. ระบบสร้างรายการ UsageLog ด้วย user_type = 'guest' พร้อมบันทึก start_time<br>4. ระบบเปลี่ยนสถานะ Computer เป็น IN_USE<br>5. ระบบแสดงหน้า Timer |
| **Alternate Flow** | ไม่มี (ไม่ต้องผ่าน UBU API) |
| **เงื่อนไขหลังสิ้นสุด** | UsageLog ถูกสร้างพร้อม user_type = 'guest' สถานะ Computer เปลี่ยนเป็น IN_USE |

---

### UC-03: ดูสถานะเซสชัน (Timer)

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-03 |
| **ชื่อ Use Case** | ดูสถานะเซสชัน (Timer) |
| **Actor** | ผู้ใช้งาน (นักศึกษา / บุคลากร / Guest) |
| **เงื่อนไขก่อนเริ่ม** | Check-in สำเร็จแล้ว มี UsageLog ที่ end_time = null |
| **เหตุกระตุ้น** | ระบบ redirect มายังหน้า Timer หลัง Check-in สำเร็จ |
| **Main Flow** | 1. ระบบโหลด UsageLog ที่ active ของเครื่องนั้น<br>2. ระบบคำนวณเวลาที่ผ่านไปจาก start_time<br>3. JavaScript อัปเดตตัวนับเวลา (HH:MM:SS) ทุก 1 วินาที<br>4. หน้าจอแสดงชื่อผู้ใช้, ชื่อซอฟต์แวร์ที่ใช้, และเวลาสะสม<br>5. ผู้ใช้สามารถกดปุ่ม Check-out เพื่อสิ้นสุดเซสชัน |
| **Alternate Flow** | หากเข้าหน้า Timer ขณะเครื่อง IN_USE แต่เป็นคนละ session → ระบบแสดงสถานะ "เครื่องถูกใช้งานอยู่" |
| **เงื่อนไขหลังสิ้นสุด** | ผู้ใช้เห็นเวลาที่ใช้งานสะสมแบบเรียลไทม์ |

---

### UC-04: Check-out ออกจากระบบ

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-04 |
| **ชื่อ Use Case** | Check-out ออกจากระบบ |
| **Actor** | ผู้ใช้งาน (นักศึกษา / บุคลากร / Guest) |
| **เงื่อนไขก่อนเริ่ม** | เครื่องคอมพิวเตอร์มีสถานะ IN_USE และมี UsageLog ที่ end_time เป็น null |
| **เหตุกระตุ้น** | ผู้ใช้กดปุ่ม "เช็คเอาท์" บนหน้าจอ Kiosk |
| **Main Flow** | 1. ระบบค้นหา UsageLog ที่ active (end_time = null) ของเครื่องนั้น<br>2. ระบบบันทึก end_time = เวลาปัจจุบัน<br>3. ระบบเปลี่ยนสถานะ Computer กลับเป็น AVAILABLE<br>4. ระบบ redirect ไปยังหน้าแบบประเมิน Feedback |
| **Alternate Flow** | 1a. ไม่พบ UsageLog ที่ active → ระบบแก้ไขสถานะ Computer เป็น AVAILABLE อัตโนมัติ (Ghost State Handling) |
| **เงื่อนไขหลังสิ้นสุด** | UsageLog ถูกอัปเดต end_time สถานะ Computer เปลี่ยนเป็น AVAILABLE ผู้ใช้เห็นหน้า Feedback |

---

### UC-05: ให้ Feedback หลังใช้งาน

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-05 |
| **ชื่อ Use Case** | ให้ Feedback หลังใช้งาน |
| **Actor** | ผู้ใช้งาน (นักศึกษา / บุคลากร / Guest) |
| **เงื่อนไขก่อนเริ่ม** | Check-out สำเร็จแล้ว มี UsageLog ที่บันทึก end_time แล้ว |
| **เหตุกระตุ้น** | ระบบ redirect มายังหน้า Feedback หลัง Check-out |
| **Main Flow** | 1. ระบบแสดงหน้าแบบประเมินความพึงพอใจ<br>2. ผู้ใช้เลือกคะแนน satisfaction_score (1–5 ดาว) ซึ่งเป็นช่องบังคับ<br>3. ผู้ใช้กรอก comment (ข้อเสนอแนะ ไม่บังคับ)<br>4. ระบบบันทึกคะแนนและความเห็นลงใน UsageLog<br>5. ระบบแสดงหน้าหลัก Kiosk พร้อมข้อความขอบคุณ |
| **Alternate Flow** | 2a. ผู้ใช้ข้าม Feedback → ระบบแสดงข้อความแจ้งว่า satisfaction_score เป็นช่องบังคับ |
| **เงื่อนไขหลังสิ้นสุด** | UsageLog ถูกอัปเดต satisfaction_score และ comment ระบบกลับสู่หน้าหลัก |

---

### UC-06: เข้าสู่ระบบ Admin Portal

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-06 |
| **ชื่อ Use Case** | เข้าสู่ระบบ Admin Portal |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | มีบัญชีผู้ดูแลระบบในระบบ และบัญชีมีสถานะ is_active = True |
| **เหตุกระตุ้น** | Admin เปิด URL `/admin-portal/login/` |
| **Main Flow** | 1. ระบบแสดงหน้า Login (Username / Password)<br>2. Admin กรอก Username และ Password<br>3. ระบบยืนยันตัวตนด้วย Django Authentication<br>4. ระบบอัปเดต SiteConfig.admin_on_duty เป็นบัญชีที่ล็อกอิน<br>5. ระบบ redirect ไปยังหน้า Monitor Dashboard |
| **Alternate Flow** | 3a. Username หรือ Password ไม่ถูกต้อง → ระบบแสดงข้อความแจ้งเตือน ไม่อนุญาตให้เข้าระบบ |
| **เงื่อนไขหลังสิ้นสุด** | Admin ล็อกอินสำเร็จ session ถูกสร้าง สามารถเข้าถึง Admin Portal ได้ทุกหน้า |

---

### UC-07: Monitor Dashboard แบบ Real-time

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-07 |
| **ชื่อ Use Case** | ดู Monitor Dashboard แบบ Real-time |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินเข้า Admin Portal แล้ว |
| **เหตุกระตุ้น** | Admin เปิดหน้า Monitor Dashboard |
| **Main Flow** | 1. ระบบแสดงหน้า Dashboard พร้อมสถานะเครื่องทุกเครื่อง<br>2. JavaScript ส่ง AJAX Request ไปยัง `/admin-portal/api/monitor/data/` ทุก 3 วินาที<br>3. API ตอบกลับข้อมูล JSON (สถานะ, ผู้ใช้, เวลาสะสม, เวลาจองถัดไป)<br>4. UI อัปเดตสถานะโดยไม่ต้อง Refresh หน้าเว็บ<br>5. Admin เห็นสถิติสรุป (ว่าง, ใช้งาน, จอง, แจ้งซ่อม) และรายการจองที่รออยู่ |
| **Alternate Flow** | ไม่มี |
| **เงื่อนไขหลังสิ้นสุด** | Admin เห็นข้อมูลสถานะเครื่องทุกเครื่องที่ทันสมัย |

---

### UC-08: Force Check-in / Force Check-out แทนผู้ใช้

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-08 |
| **ชื่อ Use Case** | Force Check-in / Force Check-out แทนผู้ใช้ |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว และอยู่ที่หน้า Monitor Dashboard |
| **เหตุกระตุ้น** | Admin กดปุ่ม "Check-in" หรือ "Force Logout" บนการ์ดเครื่องใดเครื่องหนึ่ง |
| **Main Flow (Force Check-in)** | 1. Admin กรอกชื่อผู้ใช้, ประเภท, คณะสำหรับเครื่องที่ต้องการ<br>2. ระบบสร้าง UsageLog ด้วยข้อมูลที่กรอก<br>3. ระบบเปลี่ยนสถานะ Computer เป็น IN_USE |
| **Main Flow (Force Check-out)** | 1. Admin กดปุ่ม Force Logout บนเครื่องที่มีสถานะ IN_USE<br>2. ระบบบันทึก end_time = เวลาปัจจุบันลงใน UsageLog<br>3. ระบบเปลี่ยนสถานะ Computer เป็น AVAILABLE |
| **Alternate Flow** | ไม่มี |
| **เงื่อนไขหลังสิ้นสุด** | สถานะเครื่องและ UsageLog ถูกอัปเดตโดย Admin |

---

### UC-09: จัดการการ Booking

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-09 |
| **ชื่อ Use Case** | จัดการการ Booking |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว และระบบ Booking เปิดใช้งาน (booking_enabled = True) |
| **เหตุกระตุ้น** | Admin กดเพิ่มการจองใหม่หรือเปลี่ยนสถานะการจอง |
| **Main Flow** | 1. Admin กรอกข้อมูลการจอง (รหัสผู้ใช้, เครื่อง, วันที่, เวลาเริ่ม-สิ้นสุด)<br>2. ระบบตรวจสอบความถูกต้อง (เวลาไม่ย้อนหลัง, start_time < end_time)<br>3. ระบบสร้าง Booking ด้วยสถานะ PENDING<br>4. Admin อนุมัติ (APPROVED) หรือปฏิเสธ (REJECTED) การจอง<br>5. ระบบอัปเดตสถานะ Computer เป็น RESERVED เมื่อ Booking ถูก Approve |
| **Alternate Flow** | 2a. ข้อมูลไม่ถูกต้อง → ระบบแสดงข้อความ Error ภาษาไทย |
| **เงื่อนไขหลังสิ้นสุด** | Booking ถูกสร้างและอัปเดตสถานะสำเร็จ |

---

### UC-10: นำเข้า Booking จาก CSV

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-10 |
| **ชื่อ Use Case** | นำเข้าข้อมูล Booking จากไฟล์ CSV |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว มีไฟล์ CSV ที่มีหัวคอลัมน์ "วันที่", "เวลา", "ผู้จอง", "เครื่อง" |
| **เหตุกระตุ้น** | Admin เลือก Import CSV ในหน้า Booking |
| **Main Flow** | 1. Admin เลือกไฟล์ CSV (ขนาดไม่เกิน 5 MB)<br>2. ระบบตรวจสอบนามสกุลไฟล์ (.csv) และขนาดไฟล์<br>3. ระบบอ่านและแปลงข้อมูลแต่ละแถว (รองรับรูปแบบวันที่ DD/MM/YYYY และ YYYY-MM-DD)<br>4. ระบบสร้าง Booking ทุกรายการด้วยสถานะ APPROVED อัตโนมัติ<br>5. ระบบแสดงสรุปผลการนำเข้า (จำนวนสำเร็จ / ล้มเหลว) |
| **Alternate Flow** | 2a. ไฟล์ไม่ใช่ .csv หรือขนาดเกิน 5 MB → ระบบแสดงข้อความ Error<br>3a. แถวข้อมูลมีรูปแบบไม่ถูกต้อง → ข้ามแถวนั้น แสดงจำนวนแถวที่ล้มเหลว |
| **เงื่อนไขหลังสิ้นสุด** | Booking ทุกรายการที่อ่านได้ถูกบันทึกลงฐานข้อมูลด้วยสถานะ APPROVED |

---

### UC-11: จัดการเครื่องคอมพิวเตอร์

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-11 |
| **ชื่อ Use Case** | จัดการเครื่องคอมพิวเตอร์ (เพิ่ม / แก้ไข / ลบ) |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว |
| **เหตุกระตุ้น** | Admin เปิดหน้า Manage PC |
| **Main Flow** | 1. ระบบแสดงรายการเครื่องคอมพิวเตอร์ทั้งหมดพร้อมสถานะและซอฟต์แวร์ที่ผูกไว้<br>2. Admin กรอกชื่อเครื่อง (unique), กำหนดสถานะ, เลือกซอฟต์แวร์ประจำเครื่อง (ถ้ามี)<br>3. ระบบตรวจสอบว่าชื่อเครื่องไม่ซ้ำกับเครื่องอื่น<br>4. ระบบบันทึกข้อมูล Computer ลงฐานข้อมูล |
| **Alternate Flow** | 3a. ชื่อเครื่องซ้ำกัน → ระบบแสดง Error "ชื่อเครื่องนี้มีอยู่แล้ว" |
| **เงื่อนไขหลังสิ้นสุด** | ข้อมูล Computer ในฐานข้อมูลถูกอัปเดตตามที่ Admin กำหนด |

---

### UC-12: จัดการซอฟต์แวร์และเครื่องมือ AI

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-12 |
| **ชื่อ Use Case** | จัดการซอฟต์แวร์และเครื่องมือ AI |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว |
| **เหตุกระตุ้น** | Admin เปิดหน้า Manage Software |
| **Main Flow** | 1. ระบบแสดงรายการซอฟต์แวร์ทั้งหมดพร้อมสถิติ (จำนวน Software / AI Tool)<br>2. Admin กรอกชื่อซอฟต์แวร์, เวอร์ชัน, ประเภท (Software / AI Tool), วันหมดอายุ<br>3. ระบบบันทึกข้อมูล Software ลงฐานข้อมูล<br>4. Admin สามารถแก้ไขหรือลบรายการซอฟต์แวร์ที่มีอยู่ |
| **Alternate Flow** | ไม่มี |
| **เงื่อนไขหลังสิ้นสุด** | รายการซอฟต์แวร์ในฐานข้อมูลถูกอัปเดต พร้อมสถิติแสดงจำนวน Software และ AI Tool |

---

### UC-13: ดูรายงานและ Export CSV

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-13 |
| **ชื่อ Use Case** | ดูรายงานและ Export CSV |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว และมีข้อมูล UsageLog อยู่ในระบบ |
| **เหตุกระตุ้น** | Admin เปิดหน้า Reports |
| **Main Flow** | 1. ระบบแสดงสถิติสรุป (จำนวนผู้ใช้ทั้งหมด, นักศึกษา, บุคลากร, Guest)<br>2. Admin เลือกตัวกรอง (ประเภทผู้ใช้, ช่วงวันที่, โหมดรายงาน)<br>3. ระบบ Query ข้อมูล UsageLog ตามเงื่อนไขที่เลือก<br>4. Admin กดปุ่ม Export CSV<br>5. ระบบสร้างไฟล์ CSV (13 คอลัมน์, UTF-8 BOM) ให้ดาวน์โหลด |
| **Alternate Flow** | ไม่มีข้อมูลในช่วงที่เลือก → ระบบแสดงตารางว่าง |
| **เงื่อนไขหลังสิ้นสุด** | Admin ได้รับไฟล์ CSV ที่เปิดได้ด้วย Microsoft Excel พร้อมภาษาไทย |

---

### UC-14: ตั้งค่าระบบและจัดการผู้ดูแล

| หัวข้อ | รายละเอียด |
| :--- | :--- |
| **Use Case ID** | UC-14 |
| **ชื่อ Use Case** | ตั้งค่าระบบและจัดการบัญชีผู้ดูแล |
| **Actor** | ผู้ดูแลระบบ (Admin) |
| **เงื่อนไขก่อนเริ่ม** | Admin ล็อกอินแล้ว |
| **เหตุกระตุ้น** | Admin เปิดหน้า System Config หรือหน้า Manage Users |
| **Main Flow (ตั้งค่าระบบ)** | 1. Admin แก้ไขค่าตั้งต้น (ชื่อห้อง, สถานที่, is_open, booking_enabled, announcement)<br>2. ระบบบันทึก SiteConfig ลงฐานข้อมูล<br>3. การเปลี่ยนแปลงมีผลทันทีบนหน้าจอ Kiosk |
| **Main Flow (จัดการผู้ดูแล)** | 1. Admin เพิ่มบัญชีใหม่ (username, email, password)<br>2. Admin แก้ไขหรือปิดใช้งาน (is_active) บัญชีที่มีอยู่<br>3. Admin ลบบัญชี (ไม่สามารถลบบัญชีตนเองได้)<br>4. ระบบบันทึกการเปลี่ยนแปลง |
| **Alternate Flow** | ลบบัญชีตนเอง → ระบบแสดงข้อความ Error "ไม่สามารถลบบัญชีตนเองได้" |
| **เงื่อนไขหลังสิ้นสุด** | SiteConfig หรือข้อมูลบัญชีผู้ดูแลถูกอัปเดตตามที่กำหนด |

---

## **2.6 User Story**

User Story แสดงมุมมองความต้องการของผู้ใช้งานแต่ละกลุ่ม ในรูปแบบ "ในฐานะ [บทบาท] ฉันต้องการ [ฟังก์ชัน] เพื่อ [เหตุผล]"

### กลุ่มที่ 1: นักศึกษาและบุคลากร (ผู้ใช้งานภายใน)

| US ID | User Story | ความสำคัญ |
| :---: | :--- | :---: |
| US-01 | ในฐานะนักศึกษา ฉันต้องการ Check-in ด้วยรหัส UBU WiFi เพื่อไม่ต้องกรอกข้อมูลส่วนตัวซ้ำซ้อนทุกครั้ง | สูง |
| US-02 | ในฐานะนักศึกษา ฉันต้องการเห็นเวลาที่ใช้งานสะสมแบบ Real-time เพื่อบริหารเวลาการใช้ห้องปฏิบัติการ | สูง |
| US-03 | ในฐานะนักศึกษา ฉันต้องการ Check-out ออกจากระบบได้อย่างรวดเร็ว เพื่อให้คนอื่นสามารถใช้เครื่องต่อได้ | สูง |
| US-04 | ในฐานะนักศึกษา ฉันต้องการให้ Feedback หลังใช้งาน เพื่อช่วยพัฒนาบริการห้องปฏิบัติการ | ต่ำ |

### กลุ่มที่ 2: บุคคลภายนอก (Guest)

| US ID | User Story | ความสำคัญ |
| :---: | :--- | :---: |
| US-05 | ในฐานะบุคคลภายนอก ฉันต้องการ Check-in โดยไม่ต้องมีรหัส UBU WiFi เพื่อสามารถใช้บริการห้องปฏิบัติการได้ | สูง |
| US-06 | ในฐานะบุคคลภายนอก ฉันต้องการ Check-out ได้สะดวก เพื่อสิ้นสุดการใช้งานและบันทึกเวลาออก | สูง |

### กลุ่มที่ 3: ผู้ดูแลระบบ (Admin)

| US ID | User Story | ความสำคัญ |
| :---: | :--- | :---: |
| US-07 | ในฐานะผู้ดูแลระบบ ฉันต้องการเห็นสถานะเครื่องทุกเครื่องแบบ Real-time เพื่อบริหารจัดการทรัพยากรได้อย่างมีประสิทธิภาพ | สูง |
| US-08 | ในฐานะผู้ดูแลระบบ ฉันต้องการ Force Check-out เซสชันที่ค้างอยู่ได้ เพื่อจัดการกรณีที่ผู้ใช้ลืม Check-out | สูง |
| US-09 | ในฐานะผู้ดูแลระบบ ฉันต้องการสร้างการจองเครื่องล่วงหน้าได้ เพื่อจัดสรรทรัพยากรสำหรับกิจกรรมต่าง ๆ | สูง |
| US-10 | ในฐานะผู้ดูแลระบบ ฉันต้องการนำเข้าข้อมูลการจองจำนวนมากจากไฟล์ CSV เพื่อประหยัดเวลาในการกรอกข้อมูลทีละรายการ | กลาง |
| US-11 | ในฐานะผู้ดูแลระบบ ฉันต้องการเพิ่ม แก้ไข และลบข้อมูลเครื่องคอมพิวเตอร์ได้ เพื่อให้ฐานข้อมูลตรงกับสภาพห้องปฏิบัติการจริง | สูง |
| US-12 | ในฐานะผู้ดูแลระบบ ฉันต้องการจัดการรายการซอฟต์แวร์และเครื่องมือ AI เพื่อให้นักศึกษารู้ว่าเครื่องใดมีซอฟต์แวร์อะไรบ้าง | กลาง |
| US-13 | ในฐานะผู้ดูแลระบบ ฉันต้องการดูรายงานสถิติการใช้งานและส่งออกเป็น CSV เพื่อนำไปวิเคราะห์และรายงานต่อผู้บริหาร | สูง |
| US-14 | ในฐานะผู้ดูแลระบบ ฉันต้องการตั้งค่าระบบเช่น เปิด/ปิดบริการ หรือแก้ไขประกาศ เพื่อสื่อสารข้อมูลสำคัญแก่ผู้ใช้งาน | กลาง |
| US-15 | ในฐานะผู้ดูแลระบบ ฉันต้องการจัดการบัญชีผู้ดูแลระบบอื่น เพื่อควบคุมสิทธิ์การเข้าถึงระบบ | สูง |

---

## **2.7 สรุปความต้องการของระบบ**

จากการวิเคราะห์ความต้องการพบว่าระบบ CKLab Management System มีความต้องการเชิงหน้าที่ทั้งสิ้น 14 รายการ (FR-01 ถึง FR-14) ครอบคลุม 7 กลุ่มฟังก์ชันการทำงานหลัก และมีความต้องการเชิงไม่ใช่หน้าที่ 6 รายการ (NFR-01 ถึง NFR-06) โดย Use Case ทั้งหมด 14 รายการ (UC-01 ถึง UC-14) ครอบคลุมทั้ง 2 กลุ่มผู้ใช้งาน ดังแสดงในตารางสรุปด้านล่าง

| กลุ่มฟังก์ชัน | จำนวน FR | Use Case ที่เกี่ยวข้อง |
| :--- | :---: | :--- |
| Kiosk (Check-in/out/Feedback) | 5 | UC-01, UC-02, UC-03, UC-04, UC-05 |
| Monitor Dashboard | 2 | UC-07, UC-08 |
| Booking | 2 | UC-09, UC-10 |
| PC Management | 1 | UC-11 |
| Software Management | 1 | UC-12 |
| Reports & Analytics | 1 | UC-13 |
| System Config & User Management | 2 | UC-06, UC-14 |
| **รวม** | **14** | **14 Use Cases** |

*ตารางที่ 2.1 สรุปความต้องการเชิงหน้าที่และ Use Case ของระบบ CKLab จำแนกตามกลุ่มฟังก์ชันการทำงาน*
